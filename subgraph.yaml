specVersion: 1.0.0
indexerHints:
  prune: auto
schema:
  file: ./schema.graphql
dataSources:
  - kind: ethereum
    name: BondingMarket.sol
    network: sepolia
    source:
      address: "0xac1eee2d5a1fdaec505ecf904c240541b548a1ee"
      abi: BondingMarket.sol
      startBlock: 0
    mapping:
      kind: ethereum/events
      apiVersion: 0.0.7
      language: wasm/assemblyscript
      entities:
        - BetPlaced
        - OwnerWithdrawn
        - OwnershipTransferred
        - ResultDeclared
        - RewardClaimed
      abis:
        - name: BondingMarket.sol
          file: ./abis/BondingMarket.sol.json
      eventHandlers:
        - event: BetPlaced(indexed address,indexed uint256,uint256)
          handler: handleBetPlaced
        - event: OwnerWithdrawn(uint256)
          handler: handleOwnerWithdrawn
        - event: OwnershipTransferred(indexed address,indexed address)
          handler: handleOwnershipTransferred
        - event: ResultDeclared(indexed uint256)
          handler: handleResultDeclared
        - event: RewardClaimed(indexed address,uint256)
          handler: handleRewardClaimed
      file: ./src/bonding-market-sol.ts
